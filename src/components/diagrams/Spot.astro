---
// A floor spot to occupy: a dotted mustard ring.
interface Props {
  x: number;
  y: number;
}

const { x, y } = Astro.props;
---

<circle
  cx={x}
  cy={y}
  r="14"
  fill="none"
  stroke="#d97706"
  stroke-width="2.5"
  stroke-dasharray="0.1 6.5"
  stroke-linecap="round"
/>
